name: E2E

on:
  workflow_dispatch:
  schedule:
    # Nightly at 02:00 UTC
    - cron: '0 2 * * *'

permissions:
  contents: read

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: '1.25'
          cache-dependency-path: backend/go.sum

      - name: Build e2e stack
        run: |
          docker compose -f docker/docker-compose.e2e.yml build

      - name: Start e2e stack
        run: |
          docker compose -f docker/docker-compose.e2e.yml up -d

      - name: Bootstrap Keycloak realm
        env:
          APP_ENV: test
          ALLOW_DEV_SETUP: "true"
          CREATE_DEMO_USER: "false"
          KEYCLOAK_URL: http://localhost:8083
          KEYCLOAK_ADMIN: admin
          KEYCLOAK_ADMIN_PASSWORD: admin
          KEYCLOAK_REALM: freecloud-e2e
          KEYCLOAK_CLIENT_SECRET: freecloud-secret
        run: |
          echo "Waiting for Keycloak admin API at http://localhost:8083..."
          for i in $(seq 1 60); do
            if curl -sf http://localhost:8083/realms/master >/dev/null 2>&1; then
              echo "Keycloak ready."
              break
            fi
            echo "  attempt $i/60..."
            sleep 2
          done
          curl -sf http://localhost:8083/realms/master >/dev/null
          bash backend/cmd/scripts/setup_realm.sh

      - name: Wait for backend to be ready
        run: |
          echo "Waiting for backend at http://localhost:8085/healthz..."
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8085/healthz >/dev/null 2>&1; then
              echo "Backend ready."
              break
            fi
            echo "  attempt $i/30..."
            sleep 5
          done
          curl -sf http://localhost:8085/healthz || (echo "Backend not ready after 150s." && exit 1)

      - name: Run e2e tests
        env:
          E2E_BACKEND_URL: http://localhost:8085
          E2E_SCIM_TOKEN: e2e-scim-token
          E2E_WEBHOOK_SECRET: e2e-webhook-secret
        run: |
          cd backend && go test -tags=e2e -v -timeout 5m ./internal/e2e/...

      - name: Dump logs on failure
        if: failure()
        run: |
          docker compose -f docker/docker-compose.e2e.yml logs --tail=100

      - name: Tear down stack
        if: always()
        run: |
          docker compose -f docker/docker-compose.e2e.yml down -v
