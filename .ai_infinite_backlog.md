# FreeCloud — Infinite Production Backlog

## Dear Future Me

**Cycle 2 (2026-07-17):** CI hardened; leader unlock covered under real Postgres path.

**Shipped this cycle:**
- `backend/internal/leader/integration_test.go` — real-pool unlock failover + single-leader race (`-tags=integration`, wired into `make test-db`).
- CI: **staticcheck** and **govulncheck** are hard-fail gates (npm audit stays advisory).
- `docs/SECRETS.md` — documents `FLEET_WEBHOOK_SECRET` dual use for device-cookie HMAC.

**Also landed since cycle 1 (outside loop ticks but scorecard-relevant):**
- `c20ac96` pool MaxConns fix for 5 electors (HA e2e green).
- Verify + E2E green on main after that fix.

**Tests:** `go test ./internal/leader/` green; integration tests skip locally without `TEST_DATABASE_URL` (CI db-integration runs them via `make test-db`). staticcheck clean; govulncheck: no vulns.

**Next focus:** E2E smoke on PR path; SPI cookie mint e2e; README structure refresh; optional DEVICE_COOKIE_SECRET split.

---

## Production Readiness Scorecard

| Area | Status | Evidence |
|---|---|---|
| Backend unit tests | PASS | `go test ./...` green |
| Frontend vitest | PASS | 44 tests prior; verify workflow typecheck/build |
| AuthZ / multi-tenant gates | PASS* | Epic A + portal app org + API token org-role |
| Device identity spoofing | PASS | HMAC v1 cookie + evaluate verify |
| BFF path traversal | PASS | sanitize + tests |
| Leader election unlock | PASS | explicit unlock + **integration test** (this cycle) |
| DB pool vs leaders | PASS | MaxConns ≥ 16 (`c20ac96`) |
| Review schedule runner | PASS | leader-gated runner |
| SCIM pagination totalResults | PASS | COUNT(*) + fixed conformance fakes |
| Provisioning SSRF | PASS | allowlist; private HTTP only in test/dev |
| Makefile migrate DX | PASS | `go run ./cmd/server migrate` |
| CI staticcheck / govulncheck | PASS | hard-fail this cycle |
| CI npm audit | PARTIAL | advisory only (noise) |
| E2E / HA e2e | PASS | green after pool fix (dispatch 29607246047) |
| E2E on PR | FAIL | still nightly / dispatch only |
| Real VPS deploy | FAIL | blocked on operator |
| Realm-per-org hard isolation | FAIL | ADR 0005 deferred |
| Fleet teams per-org | FAIL | shared Fleet |
| Live Slack/GitHub SCIM | FAIL | contract only |
| Docs vs code | PARTIAL | SECRETS cookie note; README tree still thin |

\* Shared Keycloak realm residual remains.

---

## Done (append-only)

- 2026-07-16 `160368f` — security review closeout
- 2026-07-16 cycle1 `23fdd86` — HMAC device cookies + portal foreign-app tests
- 2026-07-17 `c20ac96` — DB pool MaxConns for 5 leaders (HA readyz)
- 2026-07-17 cycle2 — leader integration tests + hard CI staticcheck/govulncheck + SECRETS cookie docs

---

## Pending

### P0
- [ ] **Smoke CI on PR for e2e-critical paths** — optional job or path-filtered workflow for `backend/**` + `docker/**` + SPI (not full nightly duration).

### P1
- [ ] **Access-eval SPI e2e** that mints cookie via `/enrollment/device-identity` then evaluates (not only `signedDeviceID` helper).
- [ ] **README project structure** refresh (authenticator, BFF, e2e, multi-settings, leader jobs).
- [ ] **Observability:** document/export `freecloud_leader_election_is_leader` in OBSERVABILITY.md + Grafana panel.
- [ ] **ARCHITECTURE.md** mention MaxConns / leader connection budget.

### P2
- [ ] **Fleet teams org mapping** design + minimal scoping — `device_policy.go`.
- [ ] **Dedicated `DEVICE_COOKIE_SECRET`** optional env with fallback to fleet webhook secret.
- [ ] **Frontend privileged-page tests** (employees offboard smoke with mocked api).
- [ ] **handlers package split plan** (scim / devices / access).
- [ ] Promote **npm audit** to hard-fail after triage, or pin overrides for known highs.

### P3
- [ ] CSP nonce / drop `unsafe-eval` when Next allows.
- [ ] Pin next-auth off beta when stable.
- [ ] Real multi-replica VPS burn-in checklist (blocked: needs human infra).

---

## Risks / Known Gaps

- Shared Keycloak realm = soft multi-tenant (ADR 0005).
- Device cookie domain must be shared with Keycloak login host for SPI to see it.
- Cookie signing reuses `FLEET_WEBHOOK_SECRET` — rotation impacts webhook + cookies (documented).
- No production VPS evidence.
- npm audit still soft.

## Blocked

- Real VPS / multi-replica production deploy — needs operator host + DNS + secrets.
- Live GitHub/Slack SCIM sandbox credentials.
