# FreeCloud — Infinite Production Backlog

## Dear Future Me

**Cycle 3 (2026-07-17):** PR e2e smoke + real device-cookie mint path + docs.

**Shipped this cycle:**
- E2E workflow runs **single-replica e2e on PRs** (path-filtered: backend/docker/SPI); **e2e-ha stays nightly/dispatch**.
- `TestE2E_PostureEnforcement_MintDeviceCookieThenEvaluate` — enroll → device-identity mint (Origin) → evaluate with Set-Cookie value.
- README project structure refresh; OBSERVABILITY leader metric; ARCHITECTURE MaxConns / leader conn budget.

**Tests:** unit `./...` green; e2e package compiles (`-tags=e2e`).

**Next focus:** Grafana leader panel (optional); DEVICE_COOKIE_SECRET split; Fleet teams org-scoping; frontend offboard smoke test.

---

## Production Readiness Scorecard

| Area | Status | Evidence |
|---|---|---|
| Backend unit tests | PASS | `go test ./...` green |
| Frontend vitest | PASS | verify workflow |
| AuthZ / multi-tenant | PASS* | Epic A + portal + API token org-role |
| Device identity spoofing | PASS | HMAC cookie + mint→evaluate e2e |
| BFF path traversal | PASS | sanitize + tests |
| Leader unlock | PASS | unlock + integration tests |
| DB pool vs leaders | PASS | MaxConns ≥ 16 |
| CI staticcheck / govulncheck | PASS | hard-fail |
| E2E / HA e2e | PASS | green on main |
| E2E on PR | PASS | path-filtered single-replica smoke (this cycle) |
| Docs structure / secrets / arch | PASS | README + SECRETS + ARCHITECTURE + OBSERVABILITY |
| Real VPS deploy | FAIL | blocked |
| Realm-per-org | FAIL | ADR 0005 |
| Fleet teams per-org | FAIL | shared Fleet |
| Live Slack/GitHub SCIM | FAIL | contract only |
| npm audit hard-fail | PARTIAL | advisory |

\* Shared Keycloak realm residual.

---

## Done (append-only)

- 2026-07-16 `160368f` — security review closeout
- 2026-07-16 cycle1 `23fdd86` — HMAC device cookies
- 2026-07-17 `c20ac96` — pool MaxConns for leaders
- 2026-07-17 cycle2 `77b908d` — leader integration + hard CI scanners
- 2026-07-17 cycle3 — PR e2e smoke + mint-cookie e2e + docs

---

## Pending

### P1
- [ ] **Grafana panel** for `freecloud_leader_election_is_leader` in provisioned dashboard JSON.
- [ ] **Dedicated `DEVICE_COOKIE_SECRET`** optional env, fallback to `FLEET_WEBHOOK_SECRET`.
- [ ] **Alert** `LeaderlessBackgroundJob` when sum of is_leader by job is 0 for > N minutes.

### P2
- [ ] **Fleet teams org mapping** — `device_policy.go`.
- [ ] **Frontend privileged-page tests** (employees offboard smoke).
- [ ] **handlers package split plan**.
- [ ] Promote **npm audit** after triage / overrides.
- [ ] **ARCHITECTURE** security model: mention signed device cookie (not plain host ID).

### P3
- [ ] CSP nonce / drop `unsafe-eval`.
- [ ] Pin next-auth off beta.
- [ ] Real multi-replica VPS burn-in (blocked: human infra).

---

## Risks / Known Gaps

- Shared Keycloak realm = soft multi-tenant.
- Device cookie domain must be shared with Keycloak login host.
- Cookie signing still reuses `FLEET_WEBHOOK_SECRET` (documented; split pending).
- No production VPS evidence.
- npm audit still soft.

## Blocked

- Real VPS / multi-replica production deploy — needs operator host + DNS + secrets.
- Live GitHub/Slack SCIM sandbox credentials.
