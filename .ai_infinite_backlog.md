# FreeCloud — Infinite Production Backlog

## Dear Future Me

**10-loop goal batch (cycles 5–14, 2026-07-17):** Executed ten production-loop slices after cycle 4.

### Cycle 5 — Fleet team org mapping
- Migration046 `fleet_team_orgs`; CreateTeam prefixes `{orgID}/name` + inserts mapping
- ListTeams filters non–system-admin by mapping; Assign/Move gate via `requireFleetTeamInCallerOrg`
- Tests: RecordsOrgMapping, NonAdminFilters, fakeDB `*int` Scan

### Cycle 6 — Frontend offboard smoke
- `frontend/src/lib/offboard.test.ts` — BFF POST + ApiError on 403

### Cycle 7 — Handlers ownership map
- `docs/HANDLERS_OWNERSHIP.md` domain table + split order

### Cycle 8 — Secrets init / env docs
- `docker/init-secrets/generate.sh` documents optional DEVICE_COOKIE_SECRET
- `.env.prod.example` optional DEVICE_COOKIE_SECRET lines

### Cycle 9 — PARITY honesty
- Fleet teams/policies rows note multi-tenant limits

### Cycle 10 — CreateTeam validation
- Name max length 120 + unit test

### Cycle 11 — SECURITY.md fleet teams
- Document fleet_team_orgs multi-tenant model

### Cycle 12 — README tree
- Point at HANDLERS_OWNERSHIP / docs tree cleanup

### Cycle 13 — Fake DB Scan completeness
- `*int` / `*int64` in fakeQueryRows (unblocks org team list tests)

### Cycle 14 — Full verify + baton refresh
- `go test ./...` + frontend vitest (46 tests) green; Pending replenished

**Tests:** backend full unit green; frontend 46 tests; evidence under session scratch.

**Next focus:** MoveHost/Assign e2e with mapped teams; helpdesk PermManagePolicies debate; npm audit hard-fail if desired.

---

## Production Readiness Scorecard

| Area | Status | Evidence |
|---|---|---|
| Backend unit tests | PASS | `go test ./...` |
| Frontend tests | PASS | 46 vitest |
| Fleet teams multi-tenant | PASS | fleet_team_orgs + gates (cycles 5+) |
| Device identity | PASS | HMAC + optional DEVICE_COOKIE_SECRET |
| Leader HA / pool / unlock | PASS | prior cycles |
| CI hard scanners | PASS | staticcheck + govulncheck |
| E2E on PR | PASS | path-filtered |
| Real VPS | FAIL | blocked |
| Realm-per-org | FAIL | ADR 0005 |
| npm audit hard-fail | PARTIAL | 0 high vulnerabilities last check; still advisory |

---

## Done (append-only)

- 2026-07-16 `160368f` — security review closeout
- 2026-07-16 cycle1 `23fdd86` — HMAC device cookies
- 2026-07-17 `c20ac96` — pool MaxConns
- 2026-07-17 cycle2 `77b908d` — leader integration + hard CI
- 2026-07-17 cycle3 `fcb4de0` — PR e2e + mint-cookie e2e
- 2026-07-17 cycle4 `d4a11b6` — DEVICE_COOKIE_SECRET + Grafana/alert
- 2026-07-17 cycles 5–14 — fleet_team_orgs, offboard vitest, ownership map, secrets docs, PARITY/SECURITY, name limit, fakeDB Scan, full verify

---

## Pending

### P1
- [ ] **E2e**: create team → mapping row → non-admin ListTeams only sees own (live stack).
- [ ] **Backfill** unmapped Fleet teams into `fleet_team_orgs` for Default Org (optional operator SQL in DEPLOYMENT).

### P2
- [ ] Expand helpdesk device policy permissions with org-scoped teams (product decision).
- [ ] handlers package extract `httputil` (per HANDLERS_OWNERSHIP).
- [ ] Promote npm audit if high vulns reappear.
- [ ] Grafana alert routing to Slack (ops).

### P3
- [ ] CSP nonce / drop `unsafe-eval`.
- [ ] Pin next-auth off beta.
- [ ] Real multi-replica VPS burn-in (blocked).

---

## Risks / Known Gaps

- Shared Keycloak realm + shared Fleet (app-layer team mapping only).
- Unmapped legacy Fleet teams invisible to non–system-admin.
- No production VPS evidence.

## Blocked

- Real VPS / multi-replica production deploy — needs operator host + DNS + secrets.
- Live GitHub/Slack SCIM sandbox credentials.
