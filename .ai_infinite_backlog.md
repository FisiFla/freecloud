# FreeCloud — Infinite Production Backlog

## Dear Future Me

**Cycle 4 (2026-07-17):** Device cookie secret split + HA observability.

**Shipped this cycle:**
- Optional `DEVICE_COOKIE_SECRET` (resolveSecret) with fallback to `FLEET_WEBHOOK_SECRET`; wired on handler + config helper; unit tests.
- Grafana panel **Leader election (is_leader by job)**; alert `LeaderlessBackgroundJob` (15m).
- ARCHITECTURE conditional-access section documents signed cookie + mint path; SECRETS/OBSERVABILITY updated.

**Tests:** `go test ./...` green; config + device-cookie focused tests green; Grafana JSON validates.

**Next focus:** Fleet teams org-scoping; frontend offboard smoke; handlers split plan; npm audit triage.

---

## Production Readiness Scorecard

| Area | Status | Evidence |
|---|---|---|
| Backend unit tests | PASS | `go test ./...` |
| Device identity | PASS | HMAC + optional dedicated secret + mint e2e |
| Leader unlock / pool | PASS | unlock + integration + MaxConns |
| CI staticcheck/govulncheck | PASS | hard-fail |
| E2E on PR | PASS | path-filtered single-replica |
| HA observability | PASS | metric + Grafana panel + alert (this cycle) |
| Docs (secrets/arch) | PASS | SECRETS + ARCHITECTURE cookie model |
| Real VPS | FAIL | blocked |
| Realm-per-org | FAIL | ADR 0005 |
| Fleet teams per-org | FAIL | shared Fleet |
| npm audit hard-fail | PARTIAL | advisory |

---

## Done (append-only)

- 2026-07-16 `160368f` — security review closeout
- 2026-07-16 cycle1 `23fdd86` — HMAC device cookies
- 2026-07-17 `c20ac96` — pool MaxConns
- 2026-07-17 cycle2 `77b908d` — leader integration + hard CI
- 2026-07-17 cycle3 `fcb4de0` — PR e2e + mint-cookie e2e + docs
- 2026-07-17 cycle4 — DEVICE_COOKIE_SECRET + leader Grafana/alert

---

## Pending

### P1
- [ ] **Fleet teams org mapping** — at least document + fail-closed policy or org-prefixed team names in `device_policy.go`.

### P2
- [ ] **Frontend privileged-page tests** (employees offboard smoke with mocked api).
- [ ] **handlers package split plan** (scim / devices / access ownership).
- [ ] Promote **npm audit** after triage / overrides.
- [ ] **Init-secrets / .env.prod.example** mention `DEVICE_COOKIE_SECRET` as optional.

### P3
- [ ] CSP nonce / drop `unsafe-eval`.
- [ ] Pin next-auth off beta.
- [ ] Real multi-replica VPS burn-in (blocked: human infra).

---

## Risks / Known Gaps

- Shared Keycloak realm = soft multi-tenant.
- Device cookie domain must be shared with Keycloak login host.
- No production VPS evidence.
- npm audit still soft.
- Fleet MDM teams not tenant-scoped.

## Blocked

- Real VPS / multi-replica production deploy — needs operator host + DNS + secrets.
- Live GitHub/Slack SCIM sandbox credentials.
