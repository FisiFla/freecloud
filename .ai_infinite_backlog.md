# FreeCloud — Infinite Production Backlog

## Dear Future Me

**2026-08-02 (infinite-loop cycle 14, load-path hardening):**
- Shipped: **Fleet client response-buffer cap** — `doRequest` now reads at most `maxFleetResponseBytes` (10 MiB) via `io.LimitReader` and errors "fleet response too large" beyond it, so a compromised/misconfigured Fleet can't exhaust backend memory (previously unbounded `io.ReadAll`). Complements the existing 1 MiB global request-body cap and the cycle-3 per-host software bound.
- Tests: new `TestDoRequest_RejectsOversizedResponse`; backend 18/18 pkgs green; vet clean.

**Next focus:** remaining Pending: cross-host software cursor pagination (dead surface); VPS deploy + SCIM creds (blocked, recorded).

**2026-08-02 (infinite-loop cycle 13, RBAC audit + regression):**
- Shipped: **device-actions helpdesk audit** — verified all helpdesk-reachable device endpoints (lock/restart/lock-message/clear-passcode/command-history/software/compliance) are permission-gated (PermManageDevices/PermReadCompliance), org-scoped (requireDeviceInCallerOrg/requireUserInCallerOrg), and ID-validated. Audit clean (no over-exposure). Locked it in with `TestRemoteLock_HelpdeskForeignOrgDevice404` — helpdesk in org A gets 404 + no Fleet call for an org-B device.
- Tests: backend 18/18 pkgs green; new RemoteLock regression test passes.

**Next focus:** cross-host software cursor pagination; then VPS deploy (blocked, recorded).

**2026-08-02 (infinite-loop cycle 12, dependency hygiene):**
- Shipped: **next-auth pinned to exact `5.0.0-beta.31`** (was `^5.0.0-beta.31`) — reproducible builds, no silent drift to newer betas. Note: Auth.js v5 has NO stable release yet (latest is 5.0.0-beta.32; stable latest is v4, which is a breaking rewrite — downgrading is out of scope). Revisit pin when a stable v5 ships.
- Tests: frontend 65/65; tsc clean.

**Next focus:** cross-host software cursor pagination; device-actions helpdesk audit follow-up.

**2026-08-02 (infinite-loop cycle 11, frontend security):**
- Shipped: **nonce-based CSP** — `src/lib/csp.ts` (dependency-free, unit-tested) builds the policy; `src/proxy.ts` middleware generates a per-request `crypto.randomUUID()` nonce, sets `Content-Security-Policy` (prod: `script-src 'self' 'nonce-…'` — no unsafe-inline/eval; dev keeps unsafe-eval for HMR) and exposes `x-nonce`; `layout.tsx` applies the nonce to the dark-mode inline script. CSP moved out of next.config.js (no double header). Completes the next.config.js "tighten to nonces in a later pass" TODO.
- Tests: 3 new csp unit tests; full frontend 65/65; `tsc --noEmit` clean; production `npm run build` passes.

**Next focus:** pin next-auth off beta; cross-host software cursor pagination.

**2026-08-02 (infinite-loop cycle 10, product-decision RBAC):**
- Shipped: **helpdesk PermManagePolicies** (user decision: read + mutate within org) — helpdesk roles can now create teams and assign team/app policies. Both handler families stay org-scoped via requireFleetTeamInCallerOrg / requireAppInCallerOrg, so the new capability is strictly within the caller's org (foreign-org teams still 404). Permission matrix comment documents the decision date.
- Tests: TestHasPermissionMatrix extended (helpdesk→PermManagePolicies/Devices/Compliance true; auditor→PermManagePolicies false); full backend 18/18 pkgs green.

**Next focus:** cross-host software cursor pagination; CSP nonce (P3 security).

**2026-08-02 (infinite-loop cycle 9, observability direction):**
- Shipped: **Grafana alert rules for the review-schedule runner** (docker/observability/alerts.yml) — `ReviewScheduleRunnerStalled` (no leader_ran tick in 30m) and `ReviewScheduleRunnerSkipping` (skips only, zero leader ticks for 1h). Completes the metrics→alerts loop started in cycle 4. Slack routing remains an operator-configurable receiver (no creds needed for the rules themselves). YAML validated (11 rules load).
- Tests: YAML parse check (python yaml); PromQL expressions reference the exact metric names shipped in cycle 4.

**Next focus:** cross-host software cursor pagination, or helpdesk permissions (product decision).

**2026-08-02 (infinite-loop cycle 8, continuation of user-directed refactor):**
- Shipped: **`internal/orgscope` extraction** — HANDLERS_OWNERSHIP step 2. Org-scoping gate core (`CheckResourceInOrg` + `RequireResourceInCallerOrg` + allowlist) moved to a zero-domain-dep package; handlers keep thin wrappers. 5 new orgscope unit tests (found, missing-is-404, non-allowlisted table/column rejection, nil queryer, error propagation).
- Tests: backend 18/18 pkgs green (+1 new package); vet clean.

**Next focus:** Grafana alert wiring for runner metrics; then cross-host software cursor pagination.

**2026-08-02 (infinite-loop cycle 7, user-directed refactor):**
- Shipped: **`internal/httputil` extraction** per HANDLERS_OWNERSHIP step 1 — `RespondJSON`/`RespondError`/`RespondValidationErrors` + `APIResponse`/`ValidationError`/`ValidationErrorsResponse` moved to a zero-domain-dep package. Handlers keep lowercase wrappers + type aliases (79 call sites untouched, zero behavior change). 4 new httputil unit tests (envelope, 4xx success flag, error, validation errors).
- Tests: backend 17/17 pkgs green (+1 new package); vet clean.

**Next focus:** Grafana alert wiring for runner metrics; then cross-host software cursor pagination.

**2026-08-02 (infinite-loop cycle 6, whole-gate verification):**
- Shipped: no new feature — **full-gate verification** of the 5 prior cycles. `make verify` passes end-to-end locally (go vet + go test ./... + frontend vitest + production `npm run build` incl. all 22 routes). GitHub Actions Verify green on all 7 recent commits (import-fleet-teams -apply → BFF size guard); E2E workflow green.
- Structural pivot: evidence source changed from touched-package tests to the whole-repo gate + CI (caught no regressions — integration surface clean).

**Next focus:** extract `httputil` from handlers (Go refactor), then Grafana alert wiring for runner metrics.

**2026-08-02 (infinite-loop cycle 5, frontend pivot):**
- Shipped: **BFF response-size guard** — `frontend/src/app/api/v1/[...path]/route.ts` no longer buffers unbounded backend responses; `readBoundedBody` aborts at `BFF_MAX_RESPONSE_BYTES` (default 50 MiB, env-tunable, documented in `.env.example`). Returns 502 "backend response too large" on abort. Structural pivot: first frontend/TS boundary change this loop (previous cycles were Go).
- Tests: BFF route tests 17 → 21 (502 guard + 3 readBoundedBody units); full frontend 62 tests pass; `tsc --noEmit` clean.

**Next focus:** extract `httputil` from handlers (Go); then Grafana alert wiring for runner metrics.

**2026-08-02 (infinite-loop cycle 4):**
- Shipped: **review-schedule runner observability** — Prometheus counters `freecloud_review_schedule_ticks_total{outcome}` (leader_ran / skipped_not_leader) and `freecloud_review_schedule_fired_total`, wired into `StartReviewScheduleRunner` + `fireReviewSchedule`. Follows the repo `freecloud_<component>_<metric>` convention. Tests via prometheus/testutil (counter increment + label stability).
- Note: `GetHosts` is dead interface surface (no production caller) — bounding it deferred; noted in Pending.
- Tests: backend 16 pkgs green; vet clean; 2 new metrics tests.

**Next focus:** extract `httputil` from handlers (HANDLERS_OWNERSHIP); then GetHosts bound once a caller exists, or BFF response-size guard.

**2026-08-02 (infinite-loop cycle 3):**
- Shipped: **bounded host software inventory** — `GetHostSoftware` now caps per-host software at `MaxSoftwarePerHost` (1000) with a warn log on truncation, so pathological package inventories can't balloon memory/API responses (real Fleet pages this; bound guards upstream shape). Test: oversized list truncated to exactly the cap.
- Also completed the **PATCH/body ID audit** — apps/device_actions/device_policy/mfa/orgs/portal/provisioning/review_schedules/approvals/scim/groups all validate body+path IDs (prior H01–O10 loops were thorough); marked backlog item done.
- Tests: backend 16 pkgs green; fleet client tests incl. new truncation test.

**Next focus:** extract `httputil` from handlers (HANDLERS_OWNERSHIP), then review-schedule runner metrics.

**2026-08-02 (infinite-loop cycle 2):**
- Shipped: **UpdateReviewSchedule input hardening** — optional `name` field now trimmed + validated (non-empty, ≤200 chars), and body validation moved BEFORE DB access (cheap 400s, defense-in-depth). 3 new handler tests (empty/overlong/valid).
- Tests: backend 16 pkgs green; handlers package incl. 3 new update-validation tests.

**Next focus:** remaining PATCH/body ID audit, then device software cursor pagination.

**2026-08-02 (infinite-loop cycle, offline sync):**
- Pulled local repo 106 commits forward; merged 5 dependabot PRs (react 19.2.8, react-dom 19.2.8 sync, jest-dom 7, postcss, plugin-react, lucide-react); fixed CI (react-dom version mismatch, matchMedia stub, golang.org/x/text CVE).
- Shipped: **import-fleet-teams `-apply` live DB mode** — atomic tx upsert via `ApplyMappingsTx` (parameterized, no SQL interpolation), `TEST_DATABASE_URL`-backed integration tests (insert + upsert + rollback-on-error). CLI default still prints SQL.
- Tests: backend 16 pkgs green; fleetteams unit green; DB-backed apply tests run in CI db-integration job.

**Next focus:** real VPS prod-up; dual-replica soak; live connector creds.

### Cycles 5–14 (commit `9388cc0`)
1. Fleet `fleet_team_orgs` Migration046 + CreateTeam mapping/namespace
2. ListTeams org filter for non–system-admin
3. Frontend offboard BFF smoke tests
4. `docs/HANDLERS_OWNERSHIP.md`
5. secrets-init notes for DEVICE_COOKIE_SECRET
6. PARITY Fleet multi-tenant honesty
7. CreateTeam name ≤120
8. SECURITY.md fleet teams model
9. README → ownership map
10. fakeDB `*int` Scan support

### Cycles 15–24 (this goal verification + hard gates)
11. AssignTeamPolicy foreign-org 404 test (no Fleet call)
12. MoveHostToTeam foreign-org 404 test
13. ListTeams system-admin sees unmapped teams
14. CreateTeam no-org-context 403
15. Migration046 registration unit test
16. `.env.prod.example` DEVICE_COOKIE_SECRET optional docs
17. DEPLOYMENT.md fleet_team_orgs backfill SQL
18. offboard test asserts same-origin BFF URL (not absolute API host)
19. RunDueReviewSchedules nil-DB + nextRunFromCadence unit tests
20. Full `go test ./...` + frontend 46 vitest green (SCRATCH evidence)

**Tests:** backend all packages green; frontend 46 tests; evidence in goal scratch.

**Next focus:** real VPS prod-up; dual-replica soak; live connector creds. Local e2e+backup drill done 2026-07-18.

---

## Production Readiness Scorecard

| Area | Status | Evidence |
|---|---|---|
| Backend unit tests | PASS | `go test ./...` cycle 24 |
| Frontend tests | PASS | vitest (BFF path tests expanded) |
| Fleet teams multi-tenant | PASS | mapping + list filter + mutation 404 gates + `-apply` operator tooling |
| Device identity | PASS | HMAC + optional DEVICE_COOKIE_SECRET |
| Leader HA | PASS | prior |
| CI scanners | PASS | staticcheck + govulncheck hard-fail |
| E2E on PR | PASS | path-filtered |
| Real VPS | FAIL | blocked (local Docker e2e+backup drill PASS 2026-07-18) |
| Realm-per-org | FAIL | ADR 0005 |
| npm audit hard-fail | PARTIAL | advisory |

---

## Done (append-only)

- 2026-07-16 `160368f` — security review closeout
- 2026-07-16 cycle1 `23fdd86` — HMAC device cookies
- 2026-07-17 `c20ac96` — pool MaxConns
- 2026-07-17 cycle2 `77b908d` — leader integration + hard CI
- 2026-07-17 cycle3 `fcb4de0` — PR e2e + mint-cookie e2e
- 2026-07-17 cycle4 `d4a11b6` — DEVICE_COOKIE_SECRET + Grafana/alert
- 2026-07-17 `9388cc0` — cycles 5–14 fleet teams + loop batch
- 2026-07-17 cycles 15–24 — foreign-team gates, migration test, DEPLOYMENT backfill, schedule unit tests, full verify

- 2026-07-17 G01–G10 batch commits (6388806..0ec19e2) — team name sanitize, hostId trim, ListTeams org fail-closed, path tests, SIEM leader, BFF/requireMFA (see git); baton rewrite fafc273 corrected below for append-only goal H01–H10

---
- H01: AssignTeamPolicy uses ValidatePolicyID (length/path)
- H02: ValidateHostID for MoveHostToTeam (path/dot/length)
- H03: ValidateTeamDescription for CreateTeam
- H04: ListTeams stable sort by fleet team ID
- H05: fireReviewSchedule lost-race (UPDATE 0) returns nil without campaign insert
- H06: SIEM leader-gate poll-when-leader test + debug skip log
- H07: BFF rejects empty path segments after decode
- H08: getMFAStatus same-origin privileged API test
- H09: system-admin bypasses fleet_team_orgs ownership query
- H10: ParsePositiveTeamID on AssignTeamPolicy/MoveHostToTeam (overflow-safe path id)
- I01: ValidateHostID rejects control chars on MoveHostToTeam path
- I02: ValidatePolicyID rejects control chars on AssignTeamPolicy path
- I03: MoveHostToTeam enforces maxHostIDsPerMove=500 constant
- I04: ListPolicies non-admin empty inventory (production path test)
- I05: RemoteLock ValidateHostID on device path id
- I06: BFF rejects encoded slash path segments after decode
- I07: RemoteRestart ValidateHostID on device path id
- I08: fleetteams ParseMappingCSV operator bulk-import helper
- I09: RemoteClearPasscode ValidateHostID on device path id
- I10: RemoteLockWithMessage maxLockMessageLen + control-char reject; path tests
- J01: cmd/import-fleet-teams CLI for fleet_team_orgs CSV backfill
- J02: GetDeviceCommandHistory ValidateHostID + maxDeviceCommandHistory
- J03: ValidateUserID on GetDeviceSoftware path
- J04: ValidateUserID on GetUserCompliance path
- J05: BFF rejects encoded backslash path segments after decode
- J06: SIEM pollBatchSize constant on audit_logs LIMIT
- J07: review schedule runner skip+debug when not leader
- J08: fleetteams ParseMappingCSV MaxMappingCSVRows=5000
- J09: maxDueReviewSchedules constant on RunDueReviewSchedules
- J10: maxTeamDisplayNameLen on CreateTeam + device command history BFF path test
- K01: ValidateUserID on GetMFAStatus path
- K02: ValidateUserID on RequireMFA path
- K03: ValidateUserID on Offboard path
- K04: ValidateUserID on GetUser path
- K05: BFF rejects overlong path segments (>256)
- K06: ValidateHostID on Fleet enrollment callback host_id
- K07: ValidateUserID on EvaluateAccess userId
- K08: fleetteams ParseMappingCSV rejects duplicate fleet_team_id
- K09: ValidateUserID on PatchUser path
- K10: ValidateUserID fixtures + ResetPassword path test; suite green
- L01: ValidateUserID on AssignUserToGroup path
- L02: ValidateUserID + ValidateOpaqueID on UnassignUserFromGroup
- L03: ValidateUserID on AssignRealmRoleToUser path
- L04: ValidateUserID + ValidateOpaqueID on AssignApp
- L05: ValidateHostID on EvaluateAccess deviceId after unwrap
- L06: BFF rejects more than MAX_PATH_SEGMENTS path parts
- L07: maxEnrollmentTokenLen on Fleet enrollment callback
- L08: ValidateOpaqueID on AssignRealmRole roleId/roleName
- L09: ValidateUserID on AddOrgMember userId
- L10: ValidateOpaqueID on AssignUserToGroup groupId
- M01: ValidateUserID on RemoveOrgMember path
- M02: ValidateUserID on approval offboard payload
- M03: ValidateOpaqueID on Get/UpsertAppAccessPolicy appId
- M04: ValidateUserID UUID check on review schedule update/delete
- M05: ValidateUserID on RevokeAPIToken path
- M06: ValidateUserID on GetProvisioningConfig appId
- M07: BFF rejects path segments with control characters
- M08: ValidateOpaqueID on PreviewAppPolicy appId
- M09: ValidateUserID on PortalRequestAccess appId
- M10: ValidateOpaqueID on GetSAMLIdPInitiatedURL + DeleteSchedule path test
- N01: ValidateUserID on ListCampaignItems campaign id
- N02: ValidateUserID on DecideCampaignItem path ids (prod in N01 access_review)
- N03: ValidateUserID on DecideApproval path
- N04: ValidateUserID on ListOrgMembers orgId
- N05: ValidateUserID on UpsertProvisioningConfig appId
- N06: ValidateUserID on AdminDecideAccessRequest path
- N07: BFF rejects path segments with leading/trailing whitespace
- N08: ValidateUserID on ExportCampaign campaign id
- N09: ValidateOpaqueID on GetSAMLMetadata appId
- N10: ValidateOpaqueID on PortalRemoveMFAFactor credId + CompleteCampaign test
- O01: ValidateUserID on AddOrgMember orgId
- O02: ValidateUserID on ListProvisioningState appId
- O03: ValidateUserID on ResyncUser appId/userId
- O04: ValidateUserID on DryRunProvisioning userId (prod reordered in O02)
- O05: ValidateUserID on ReconcileAllHandler appId (prod in O02)
- O06: ValidateUserID on SCIM org-path bearer middleware
- O07: ValidateUserID on SCIMGetUser path id
- O08: ValidateHostID on MintDeviceCookieValue hostID
- O09: BFF rejects triple-dot path segments
- O10: ValidateOpaqueID on SCIM group path ids
- 2026-07-18 local pilot: e2e stack green; fleet team mapping e2e; backup/restore drill; PILOT_GO_LIVE.md
- 2026-08-02 offline sync: pulled 106 commits, merged 5 dependabot PRs, fixed CI (react-dom match, matchMedia stub, x/text CVE)
- 2026-08-02 cycle: import-fleet-teams `-apply` live DB mode + ApplyMappingsTx + DB-backed tests
- 2026-08-02 cycle2: UpdateReviewSchedule name trim/length validation, body-before-DB reorder + 3 tests
- 2026-08-02 cycle3: MaxSoftwarePerHost bound on GetHostSoftware + truncation test; body-ID audit completed
- 2026-08-02 cycle4: review-schedule runner metrics (ticks/fired Prometheus counters) + testutil tests
- 2026-08-02 cycle5: BFF response-size guard (readBoundedBody, 50MiB default env-tunable) + 4 tests
- 2026-08-02 cycle6: whole-gate verification — make verify local green (go+frontend+build), CI Verify green on all 7 recent commits, E2E green
- 2026-08-02 cycle7: httputil extraction (RespondJSON/RespondError/validation) per HANDLERS_OWNERSHIP + 4 unit tests
- 2026-08-02 cycle8: orgscope extraction (org-scoping gate core) per HANDLERS_OWNERSHIP step 2 + 5 unit tests
- 2026-08-02 cycle9: Grafana alerts for review-schedule runner (stalled + skipping) in docker/observability/alerts.yml
- 2026-08-02 cycle10: helpdesk PermManagePolicies (read+mutate within org) per user decision + matrix tests
- 2026-08-02 cycle11: nonce-based CSP (drop unsafe-inline/eval from script-src in prod) + 3 unit tests
- 2026-08-02 cycle12: pinned next-auth to exact 5.0.0-beta.31 (reproducible; no stable v5 exists yet)
- 2026-08-02 cycle13: helpdesk device-actions audit (clean) + TestRemoteLock_HelpdeskForeignOrgDevice404 regression
- 2026-08-02 cycle14: Fleet doRequest response-buffer cap (10MiB, io.LimitReader) + oversized-response test

## Pending

### P1
- [x] **Live e2e** create team → `fleet_team_orgs` row → ListTeams filter (e2e compose + TestE2E_FleetTeams_CreateMapsAndLists).
- [x] **Operator tooling** import-fleet-teams `-apply` live DB mode (atomic tx upsert + DB-backed tests). CLI default still prints SQL.

### P2
- [x] ValidateUserID on RemoveOrgMember and remaining approval payload paths (audit complete: all body/PATH IDs validated across handlers).
- [x] Device software inventory path: bounded per-host list (MaxSoftwarePerHost=1000 + truncation test). Cursor pagination across hosts still open (large-fleet listing).
- [x] Extract `httputil` from handlers (done: internal/httputil package with RespondJSON/RespondError/RespondValidationErrors + type aliases in handlers; step 1 of HANDLERS_OWNERSHIP).
- [x] Extract `internal/orgscope` (done: org-scoping gate core + allowlist; handlers keep thin wrappers; step 2 of HANDLERS_OWNERSHIP).
- [x] Grafana alert wiring for review-schedule runner metrics (done: ReviewScheduleRunnerStalled + ReviewScheduleRunnerSkipping in docker/observability/alerts.yml; Slack receiver optional operator config).
- [x] Review schedule runner metrics/export for non-leader skip rate (done: `freecloud_review_schedule_ticks_total{outcome}` + `_fired_total` + Grafana alerts cycle 9).
- [ ] Bound `GetHosts` list size (fleet/client.go) — NOTE: no production caller today (interface surface); bind only when a caller exists, or add a max-hosts guard defensively.
- [x] BFF response-size sanity guard in `frontend/src/app/api/v1/[...path]/route.ts` for software/compliance payloads (done: readBoundedBody, 50MiB default env-tunable, 502 on abort).
- [x] Device-actions helpdesk audit follow-up (done 2026-08-02: all helpdesk-reachable device endpoints org-scoped + ID-validated; foreign-org regression test added).
- [ ] Device software inventory: cursor pagination across hosts for large-fleet listing (open follow-up to the per-host bound).

### P3
- [x] CSP nonce / drop `unsafe-eval` (done 2026-08-02: nonce-based CSP via middleware, prod drops unsafe-inline/eval from script-src).
- [x] Pin next-auth off beta (done 2026-08-02: exact 5.0.0-beta.31 pin; revisit when a stable v5 ships — currently no stable v5 exists).
- [ ] Grafana alert routing to Slack.
- [ ] Promote npm audit to hard-fail if high vulns reappear.
- [ ] Real multi-replica VPS burn-in (blocked).

---

## Risks / Known Gaps

- Shared Keycloak realm + shared Fleet (app-layer team mapping only).
- Unmapped legacy teams invisible to non–system-admin until backfill.
- No production VPS evidence.

## Blocked

- Real VPS / multi-replica production deploy — needs operator host + DNS + secrets.
- Live GitHub/Slack SCIM sandbox credentials.
