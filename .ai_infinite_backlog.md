# FreeCloud — Infinite Production Backlog

## Dear Future Me

**Cycle 1 (2026-07-16):** First loop. Created scorecard from full-repo review residuals.

**Shipped this cycle:**
- HMAC-signed `freecloud-device-id` cookies (`MintDeviceCookieValue` / `ParseAndVerifyDeviceCookie`); access/evaluate verifies MAC and rejects bare host IDs when `FLEET_WEBHOOK_SECRET` is set.
- Regression: portal foreign-app access request → 404 (no INSERT).
- Unit tests for cookie round-trip, unsigned reject, signed allow; e2e helpers mint signed deviceIds.
- SECURITY.md documents the cookie model.

**Tests:** `go test ./...` (all packages) green; e2e package compiles with `-tags=e2e`.

**Next focus:** leader unlock real-Postgres integration test; promote soft CI gates; SIEM/schedule smoke docs; Fleet teams org-scoping design.

---

## Production Readiness Scorecard

| Area | Status | Evidence |
|---|---|---|
| Backend unit tests | PASS | `go test ./...` green after signed-cookie cycle |
| Frontend vitest | PASS | last full suite 44 tests (prior cycle) |
| AuthZ / multi-tenant gates | PASS* | Epic A isolation + portal app org check + API token org-role fix (160368f) |
| Device identity spoofing | PASS | HMAC v1 cookie + evaluate verify (this cycle) |
| BFF path traversal | PASS | 160368f sanitize + tests |
| Leader election unlock | PARTIAL | Explicit `pg_advisory_unlock` shipped; needs real-pg integration test |
| Review schedule runner | PASS | Leader-gated runner (160368f) |
| SCIM pagination totalResults | PASS | COUNT(*) (160368f) |
| Provisioning SSRF | PASS | URL allowlist (160368f) |
| Makefile migrate DX | PASS | `go run ./cmd/server migrate` |
| CI hard gates (govulncheck/npm audit) | FAIL | still `continue-on-error: true` |
| E2E on PR | FAIL | nightly only |
| Real VPS deploy | FAIL | never done — blocked on operator |
| Realm-per-org hard isolation | FAIL | deferred ADR 0005 |
| Fleet teams per-org | FAIL | shared Fleet; empty list for non-system-admin |
| Live Slack/GitHub SCIM | FAIL | contract tests only |
| Docs vs code (ARCHITECTURE) | PARTIAL | BFF + migrate fixed; README tree still thin |

\* Shared Keycloak realm residual remains (email uniqueness, global roles).

---

## Done (append-only)

- 2026-07-16 `160368f` — security review closeout (API token org-role, portal app org, BFF traversal/downloads, leader unlock, access eval any-device, schedules runner, SIEM leader, SCIM totalResults, SSRF, setup password, etc.)
- 2026-07-16 cycle1 — HMAC-signed device identity cookies + portal foreign-app regression tests

---

## Pending

### P0
- [ ] **Leader unlock integration test** against real Postgres: acquire → `releaseIfLeader` → second elector acquires without process death (`backend/internal/leader/`, `-tags=integration` or testcontainers).
- [ ] **Promote govulncheck (and/or staticcheck) to hard-fail** in `.github/workflows/verify.yml` once current findings are zero or triaged.

### P1
- [ ] **E2E smoke on PR** for backend/docker/SPI path changes (subset of posture + org isolation, not full nightly).
- [ ] **Document DEVICE cookie + FLEET_WEBHOOK_SECRET** in `docs/SECRETS.md` / DEPLOYMENT (cookie signing key reuse).
- [ ] **Access-eval SPI e2e** path that mints cookie via `/enrollment/device-identity` then evaluates (not only raw signed helper).
- [ ] **README project structure** refresh (authenticator, BFF, e2e, multi-settings).

### P2
- [ ] **Fleet teams org mapping** design + minimal scoping (namespace or `org_id` table) — `device_policy.go`.
- [ ] **Signed cookie rotation** / dedicated `DEVICE_COOKIE_SECRET` env (optional split from fleet webhook secret).
- [ ] **Frontend privileged-page tests** (employees offboard happy/error path smoke with mocked `lib/api`).
- [ ] **handlers package split plan** (scim / devices / access) — document ownership boundaries.

### P3
- [ ] CSP nonce / drop `unsafe-eval` when Next allows.
- [ ] Pin next-auth off beta when stable.
- [ ] Real multi-replica VPS burn-in checklist (blocked: needs human infra).

---

## Risks / Known Gaps

- Shared Keycloak realm = soft multi-tenant (ADR 0005).
- Device cookie domain must be shared with Keycloak login host for SPI to see it (ops topology).
- Cookie signing reuses `FLEET_WEBHOOK_SECRET` — rotation impacts both webhook + cookies.
- HA only proven in Compose e2e, not field.
- Soft CI security scanners can hide regressions.
- No production VPS evidence.

## Blocked

- Real VPS / multi-replica production deploy — needs operator host + DNS + secrets.
- Live GitHub/Slack SCIM sandbox credentials.
